<uses-permission android:name="android.permission.RECORD_AUDIO" /> goes in android manifest.xml
<uses-permission android:name="android.permission.CAMERA" /> OUR LINE

        registerForActivityResult(ActivityResultContracts.RequestPermission()) { granted ->  goes in on create in Main_Activity
            if (!granted) {
                toast("Audio access denied")
                finish()
            }
        }.launch(Manifest.permission.RECORD_AUDIO)


registerForActivityResult(ActivityResultContracts.RequestPermission()) { granted ->
    if (!granted) {
        toast("Camera access denied") // Updated toast message
        finish()
    }
}.launch(Manifest.permission.CAMERA) // Request CAMERA permission instead of RECORD_AUDIO #OUR CODE
